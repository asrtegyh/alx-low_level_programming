#include<stdio.h>

/**
 * main - printout of all digit numbers of base
 * from 0 to 10,followed by newline
 * Return: Always 0 (Success)
 */

int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
	{
		printf("%d",n);
	}
	printf("\n")
		return (0);
}

